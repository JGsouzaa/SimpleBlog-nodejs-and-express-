<div class="card">
    <div class="card-body">
        <h1>{{post.titulo}}</h1>
        <hr>
        <small>Data da publicação: {{post.data}}</small>
        <br>
        {{post.conteudo}}
    </div>
</div>